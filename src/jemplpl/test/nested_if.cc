/*
  copypasta for invoking the nested_if test.  yay.
  some day I'll come up with a better structure for these.
 */

#include <iostream>

#include "nested_if.h"

int main() {
    std::cout << nested_if();

    return 0;
}

